#include "main.h"
/**
 * _abs - Computes the absolute value of an integer.
 *
 * @a: The integer to be computed
 *
 * Return: Absolute value
 */
int _abs(int a)
{
	if (n <  0)
	{
		return (n * -1);
	}
	return (n);
}
