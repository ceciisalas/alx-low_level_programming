#include "main.h"
/**
 * print_rev - Print a string, in reverse
 * @s: string
 *
 * Return: Always 0
 */
void print_rev(char *s)
{
	int i, count, j;

	for (i = 0; i < count; i++)
	{
		rev[i] = *s[j];
		j--;
	}
	_putchar('\n');
}
