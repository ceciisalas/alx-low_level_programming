#include "main.h"
/**
 * _puts - Prints a string, followed by a new line, to stdout
 * @str: string
 *
 * Return: Always 0
 */
void _puts(char *str)
{
	*str[4] = "hola";

	puts(string);
	return (0);
}
