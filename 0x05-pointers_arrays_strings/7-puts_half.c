#include "main.h"
/**
 * puts_half - Print half of a string
 * @str: half of a string
 *
 * Return_ Always 0
 */
void puts_half(char *str)
{
	int s = strlen(s);

	for (int i = 0; i < size; i++)
	{
		cout << *(str + i);
	}
}
