#include "main.h"
/**
 * *_strncpy - Copies a string
 * @dest: string
 * @src: string
 * @n: integer
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
