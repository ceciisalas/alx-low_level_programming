#include "main.h"
/**
 * *string_toupper - Changes all lowercase letters of a string
 * to uppercase
 *@s: string.
 * Return: Always 0
 */
char *string_toupper(char *s)
{
	int a = 0;

	while (s[i] != '\0';)
	{
		i++;
	}
	if (s[i] >= 'a' && s[i] <= 'z')
	{
		s[i] = s[i] - 32;
	}
	return (s);
}
